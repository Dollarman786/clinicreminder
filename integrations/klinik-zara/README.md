# Klinik Zara + ClinicReminder

The supplied Klinik Zara website has been updated to use ClinicReminder for appointment creation and reminder scheduling.

WhatsApp contact was changed to **+601155275797**.

Before publishing, replace these placeholders in `klinik_zara_clinicreminder.html`:
- `https://YOUR-CLINICREMINDER-DOMAIN/widget/clinicreminder.js`
- `https://YOUR-PROJECT.supabase.co`
- `YOUR_KLINIK_ZARA_API_KEY`

Do not publish a real clinic API key until the API is protected by an origin allow-list/rate limit. For the first controlled test, use a dedicated test key and test patient number.
